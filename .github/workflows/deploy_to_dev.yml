name: Deploy to Dev

on:
  push:
    tags:
    - 'dev'

jobs:

  build:
    name: Deploy to Dev
    runs-on: ubuntu-latest
    steps:

    - name: Check out code
      uses: actions/checkout@v2

    - name: Debug
      env:
        LONG_SHA: ${{ github.sha }}
      run: |
        echo "$LONG_SHA"


    # - name: Deploy to dev
    #   run: |
    #     echo "Deploying to dev"

